import React from 'react';
import {
    StyleSheet,
    TextInput,
    ScrollView,
    Text,
    View,
    Button,
    SafeAreaView,
    ImageBackground,
    TouchableOpacity,
    Image
  } from 'react-native';

import { useState } from 'react-native'


export default function SaveToCameraRoll() {
  return (
    <Text>Save to Camera Roll Screen</Text>
  )
}
